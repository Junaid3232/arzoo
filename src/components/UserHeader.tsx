import React from 'react';
import styled from 'styled-components/native';
import {FlexCentered} from 'components/common/View';

const UserHeader = () => {
    return (
        <FlexCentered>

        </FlexCentered>
    );
}

export default UserHeader;

const UserImage = styled.Image`
    background-color: black;
    width: 120px;
    height: 120px;
    border-radius: 60px;
    top: -40px;
    left: -10px;
`
